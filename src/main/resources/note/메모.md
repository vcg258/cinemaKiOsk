

to 현재님(담당이 맞으신가?)
// StatisticsEntity.java
@Column(name = "date", nullable = false)
private LocalDateTime date;  // ← init.sql에 없는 컬럼인데 nullable=false

date 컬럼은 NOT NULL인데 값도 없고 DEFAULT도 없으니까 에러가 터집니다. (설명 듣고싶어요!)

해결 방법 1 : StatisticsEntity에서 date 필드 제거하거나 nullable = true로 변경
해결 방법 2 : init.sql에 date 컬럼 추가


