# 프론트엔드 연동 가이드

> 작성 기준 브랜치: `view`
> 이 문서는 컨트롤러 · 서비스 · 백엔드 담당자분들이 프론트엔드와 연동 작업 시 참고하는 가이드입니다.

---

## 1. 화면 미리보기 방법

서버 실행 후 `/preview/...` 경로로 접근하면 **실제 데이터 없이도** 뷰 레이아웃을 바로 확인할 수 있습니다.

| URL | 설명 |
|-----|------|
| `/preview/movie/list` | UC-01 상영작 목록 |
| `/preview/movie/detail` | UC-02 상영작 상세 |
| `/preview/booking/schedule` | UC-03 날짜·시간·인원 선택 |
| `/preview/booking/seat` | UC-03 좌석 선택 |
| `/preview/booking/payment` | UC-04~06 결제·포인트 |
| `/preview/booking/confirm` | UC-07 예매 완료 |
| `/preview/mypage/lookup` | UC-08 예매 조회 |
| `/preview/mypage/detail` | UC-08 예매 상세 |
| `/preview/auth/phone` | UC-05 휴대폰 인증 |
| `/preview/admin/login` | UC-11 관리자 로그인 |
| `/preview/admin/dashboard` | 관리자 대시보드 |
| `/preview/admin/stats/daily` | UC-12 일일 통계 |
| `/preview/admin/stats/monthly` | UC-13 월별 통계 |
| `/preview/admin/stats/by-day` | UC-14 요일별 통계 |
| `/preview/admin/stats/by-hour` | UC-15 시간대별 통계 |
| `/preview/admin/stats/by-movie` | UC-16 영화별 통계 |
| `/preview/admin/refund` | UC-17 환불 처리 |
| `/preview/admin/movie/form` | UC-18~19 영화 등록·수정 |
| `/preview/admin/movie/manage` | UC-20 영화 목록·삭제 |
| `/preview/admin/theater/edit` | UC-21 상영관 수정 |

> `PreviewController.java` 파일에도 동일한 목록이 주석으로 정리되어 있습니다.

---

## 2. 컨트롤러 작성 규칙

### 2-1. Model 속성 키 이름 준수

각 HTML 템플릿 파일 **상단 주석**에 `Controller → View 전달 Model 속성` 항목이 명시되어 있습니다.
`model.addAttribute()` 작성 시 **주석에 적힌 키 이름을 반드시 그대로 사용**해 주세요.
키 이름이 다를 경우 Thymeleaf에서 데이터를 읽지 못합니다.

**예시 (`movie/detail.html` 기준)**

```java
// MovieController.java
@GetMapping("/movie/{id}")
public String detail(@PathVariable Long id, Model model) {
    model.addAttribute("movie", movieService.findById(id));       // 키: "movie"
    model.addAttribute("isSoldOut", movieService.isSoldOut(id));  // 키: "isSoldOut"
    return "movie/detail";
}
```

### 2-2. URL 매핑 기준

| 컨트롤러 | 담당 URL |
|---------|---------|
| `MainController` | `GET /movie/list`, `GET /movie/{id}` |
| `BookingController` | `GET /booking/schedule`, `/booking/seat`, `/booking/payment`, `/booking/confirm` |
| `MypageController` | `GET /mypage/lookup`, `GET /mypage/booking/{id}` |
| `AuthController` | `POST /admin/login`, `GET /admin/dashboard` |
| `AdminController` | `GET /admin/stats/**`, `GET /admin/refund`, `GET /admin/movie/**`, `GET /admin/theater/**` |

### 2-3. 레이아웃 구분

| 대상 | 사용 레이아웃 |
|------|------------|
| 고객용 전체 페이지 | `layout/base.html` |
| 관리자용 전체 페이지 | `layout/admin-base.html` |
| 관리자 로그인 페이지 | 레이아웃 없음 (단독 HTML) |

---

## 3. REST API 엔드포인트 명세

프론트엔드 JS(`fetch` / `axios`)가 호출하는 엔드포인트 목록입니다.
아래 형태에 맞춰 구현해 주세요. 응답은 해당 **DTO 기준 JSON**으로 반환해 주세요.
저희 KiOsk 명세서에 가이드를 만들어놓았고, 백엔드 담당자 분들 협의 후 확정 및 수정 해주시면 그 내용 토대로 작업하겠습니다. 

### 고객 영역

| 기능 | Method | URL | 요청 파라미터 / Body | 응답 |
|------|--------|-----|-------------------|------|
| 영화 목록 조회 | GET | `/api/movies` | `?genre=&rating=&keyword=` (선택) | `List<MovieDTO>` |
| 영화 상세 조회 | GET | `/api/movies/{movieId}` | - | `MovieDTO` |
| 상영 스케줄 조회 | GET | `/api/schedules` | `?movieId=` | `List<ScheduleDTO>` |
| 예매 조회 | GET | `/api/mypage/lookup` | `?reservationId=` | `ReservationDetailsDTO` |
| 포인트 인증 | POST | `/api/auth/phone` | `{ phone }` | `MembersDTO` |
| 결제 요청 | POST | `/api/payment` | `PaymentDetailsDTO` | `PaymentDetailsDTO` |
| 예매 취소 | DELETE | `/api/reservation/{id}` | - | - |

### 관리자 영역

| 기능 | Method | URL | 요청 파라미터 / Body | 응답 |
|------|--------|-----|-------------------|------|
| 환불 조회 | GET | `/api/admin/refund` | `?reservationId=` 또는 `?phone=` | `ReservationDetailsDTO` |
| 환불 처리 | POST | `/api/admin/refund/{reservationId}` | - | - |
| 영화 등록 | POST | `/api/admin/movie` | `MovieDTO` | `MovieDTO` |
| 영화 수정 | PUT | `/api/admin/movie/{movieId}` | `MovieDTO` | `MovieDTO` |
| 상영 중지 | PATCH | `/api/admin/movie/{movieId}/stop` | - | - |
| 상영관 수정 | PUT | `/api/admin/theater/{no}` | `TheaterDTO` | `TheaterDTO` |
| 통계 조회 | GET | `/api/admin/stats/{type}` | `?startDate=&endDate=` | `List<StatisticsDTO>` |

> `{type}` : `daily` / `monthly` / `by-day` / `by-hour` / `by-movie`

---

## 4. WebSocket 연동 (UC-03 좌석 선택)

`booking/seat.html` 화면에서 실시간 좌석 동기화에 사용합니다.
`WebsocketController` 구현 시 아래 경로 참고해 주세요.

```
연결 엔드포인트 : /ws  (SockJS)
구독 경로       : /topic/seat/{scheduleId}   ← 서버 → 클라이언트 브로드캐스트
발행 경로       : /app/seat/select           ← 클라이언트 좌석 선택 시 전송
                  /app/seat/release          ← 클라이언트 이탈 / 해제 시 전송
```

메시지 형태는 `WebsocketController` 담당자(혜윰님)와 별도 협의 후 확정할 것.

---

## 5. CSS / JS 파일 작업 안내

### CSS

각 CSS 파일은 껍데기 상태입니다.(404 에러 방지) 화면 구현 시 해당 파일에 스타일을 작성할 것입니다. 

| 파일 | 담당 화면 |
|------|---------|
| `static/css/base.css` | 전역 변수 · 리셋 · 타이포그래피 |
| `static/css/components.css` | 공용 컴포넌트 (버튼, 카드, 모달 등) |
| `static/css/movie.css` | UC-01 목록 · UC-02 상세 |
| `static/css/seat.css` | UC-03 좌석 맵 |
| `static/css/payment.css` | UC-04~06 결제 단계 |
| `static/css/admin.css` | 관리자 전체 페이지 |
| `static/css/fonts.css` | Pretendard 폰트 선언 |

### JS

현재 각 HTML의 `<script>` 태그에 `th:if="${false}"` 처리가 되어 있어 **JS 파일이 없어도 서버 에러가 발생하지 않습니다.**
해당 JS 파일을 실제로 생성한 후에는 `th:if="${false}"` 부분을 제거해 주세요.

```html
<!-- 제거 전 (파일 없을 때) -->
<script th:src="@{/js/movie/list.js}" th:if="${false}"></script>

<!-- 제거 후 (파일 생성 완료 시) -->
<script th:src="@{/js/movie/list.js}"></script>
```

---

## 6. 변경 이력

| 일자 | 항목 | 내용 |
|------|------|------|
| 2026-03-15 | UC-08 조회 방식 변경 | 개인정보 보호를 위해 휴대폰 번호 조회 → **예매번호 조회** 방식으로 변경 |

---

## 7. 주의사항

- 백엔드 로직(Service, Repository)은 프론트엔드 파일에 구현하지 않습니다.
- `th:inline="javascript"` 없이 Thymeleaf 변수를 JS에서 직접 참조하는 코드는 작성하지 말아 주세요.
